#\n# @lc app=leetcode id=3729 lang=cpp\n#\n# [3729] Count Distinct Subarrays Divisible by K in Sorted Array\n#\n# @lc code=start\nclass Solution {\npublic:\n    long long numGoodSubarrays(vector<int>& nums, int k) {\n        unordered_map<int, int> prefixCount; // To keep track of mod values\n        unordered_set<int> seenPrefixMods; // To track unique prefix sums mod k\n        int n = nums.size();\n        long long count = 0;\n        int prefixSum = 0;\n        prefixCount[0] = 1; // Initial case for whole array divisible by k\n        for (int i = 0; i < n; ++i) {\n            prefixSum += nums[i];\n            int modValue = ((prefixSum % k) + k) % k; // Non-negative modulo adjustment\n            if (prefixCount[modValue] > 0 && seenPrefixMods.find(modValue) == seenPrefixMods.end()) { \n                ++count; \n                seenPrefixMods.insert(modValue); \n            } \n            ++prefixCount[modValue]; // Update count for this modValue in map\n        }\n        return count; // Return total number of distinct good subarrays found\n    } \nt};\nt# @lc code=end