# \n# @lc app=leetcode id=3640 lang=cpp\n#\n# [3640] Trionic Array II\n#\n\n# @lc code=start\nclass Solution {\npublic:\n    long long maxSumTrionic(vector<int>& nums) {\n        int n = nums.size();\n        if (n < 4) return 0; // Not enough elements for a trionic subarray\n        vector<long long> leftMax(n, LLONG_MIN);\n        vector<long long> rightMax(n, LLONG_MIN);\n        \n        // Calculate max increasing sums ending at each position for leftMax\n        for (int i = 1; i < n; ++i) {\n            if (nums[i] > nums[i - 1]) {\n                leftMax[i] = (leftMax[i - 1] == LLONG_MIN ? nums[i - 1] : leftMax[i - 1]) + nums[i];\n            } else {\n                leftMax[i] = nums[i]; // Start new increasing sequence\n            }\n        }\n        \n        // Calculate max decreasing sums starting at each position for rightMax\n        for (int i = n - 2; i >= 0; --i) {\n            if (nums[i] > nums[i + 1]) {\n                rightMax[i] = (rightMax[i + 1] == LLONG_MIN ? nums[i + 1] : rightMax[i + 1]) + nums[i];\n            } else {\n                rightMax[i] = nums[i]; // Start new decreasing sequence\n            }\n        }\n        \n        // Find maximum trionic subarray sum by evaluating potential peaks q in the middle of such arrays. \u0009long long maxTrionicSum = LLONG_MIN; \u0009for (int q = 2; q < n - 1; ++q) { \u0009    if (nums[q - 1] > nums[q] && nums[q] < nums[q + 1]) { \u0009         // Ensure current q forms a valid triplet by checking adjacent values \u0009         maxTrionicSum = max(maxTrionicSum, leftMax[q - 1] + rightMax[q + 1] + nums[q]); \u0009    } \u0009} \u0009return maxTrionicSum; \u0009} }; # @lc code=end